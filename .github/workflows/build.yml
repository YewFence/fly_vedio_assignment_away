name: Build Executables

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      ref:
        description: '要构建的版本 (tag/branch/commit SHA)'
        required: false
        default: ''
        type: string

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact_name: fly_video_assignment_away-windows.exe
            asset_name: fly_video_assignment_away-windows.exe
          - os: macos-latest
            artifact_name: fly_video_assignment_away
            asset_name: fly_video_assignment_away-macos.zip

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref || github.ref }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: |
          uv sync
          uv add pyinstaller

      - name: Build executable
        run: |
          uv run pyinstaller --onefile --name fly_video_assignment_away main.py

      - name: Rename artifact (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          mv dist/fly_video_assignment_away.exe dist/${{ matrix.artifact_name }}

      - name: Zip artifact (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          chmod +x dist/${{ matrix.artifact_name }}
          ditto -c -k --sequesterRsrc dist/${{ matrix.artifact_name }} dist/${{ matrix.asset_name }}

      - name: Verify macOS artifact is executable
        if: matrix.os == 'macos-latest'
        run: |
          mkdir artifact-check
          ditto -x -k dist/${{ matrix.asset_name }} artifact-check
          test -x artifact-check/${{ matrix.artifact_name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: dist/${{ matrix.asset_name }}

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(inputs.ref, 'v') || startsWith(github.ref, 'refs/tags/v')

    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.ref || github.ref_name }}
          files: |
            artifacts/fly_video_assignment_away-windows.exe/fly_video_assignment_away-windows.exe
            artifacts/fly_video_assignment_away-macos.zip/fly_video_assignment_away-macos.zip
          generate_release_notes: true
